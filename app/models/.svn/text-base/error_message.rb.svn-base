# This is a simple method for keeping track of errors that customers might
# have encountered or configuration problems so they can be shown to store
# administrators and customer service reps if a customer calls in who is
# experiencing problems.
class ErrorMessage < ActiveRecord::Base
end
