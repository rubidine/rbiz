<% if @tags.length == 1 and @tags.first.full_description and !@tags.first.full_description.strip.empty? -%>
<div class="tagdescription">
  <%= @tags.first.full_description %>
</div>
<% end -%>

<div class="pagination">
  <%= will_paginate @products %>
</div>

<%= render :partial=>'product_info', :collection=>@products %> 

<br clear="both"/>

<div class="pagination">
  <%= will_paginate @products %>
</div>

<div class="breadcrumbs">
  <%= render :partial=>'shared/breadcrumbs' %>
</div>

<div class="relatedtags">
  <%= render :partial=>'shared/related_tags' %>
</div>
